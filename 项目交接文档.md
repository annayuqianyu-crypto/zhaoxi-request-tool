# 朝曦家办 · 需求打磨工作台 — 项目交接文档

> 更新时间：2026-08-05 | 对应 commit：`a32f1d2`
> 架构：**纯静态前端**（无后端、无数据库）
>
> ⚠️ 若你看到的版本描述了 FastAPI / Supabase / Render，那是 2026-04 的旧文档，已作废。

---

## 一、项目概览

| 项 | 内容 |
|---|---|
| 产品名称 | 朝曦家办 · 需求打磨工作台（zhaoxi-request-tool） |
| 用途 | 内部工具。业务团队通过 AI 深度访谈，把模糊想法转成可交付 IT 的需求规格 |
| 线上地址 | https://annayuqianyu-crypto.github.io/zhaoxi-request-tool/ |
| 代码仓库 | https://github.com/annayuqianyu-crypto/zhaoxi-request-tool |
| 部署方式 | GitHub Pages，从 `main` 分支根目录直接发布（**仓库内无 CI 配置**，在 GitHub 仓库 Settings → Pages 中设置） |
| 代码规模 | `index.html` 单文件约 **4874 行**（HTML + CSS + JS 全内联） |

---

## 二、架构：为什么现在没有后端

项目经历过三次架构迁移，理解这段历史能避免走回头路：

```
① FastAPI + Supabase + Render      →  Render 免费实例 30s 超时，长对话必 502
② 前端 + Cloudflare Worker 代理     →  多一跳、多一处要维护的密钥
③ 纯静态前端，浏览器直连 LLM API    →  当前架构
```

**取舍：**

- ✅ 零运维、零成本、无冷启动、不受 30s 超时限制；部署 = `git push`
- ❌ **每位用户必须自己填 API Key**（存各自浏览器 localStorage）
- ❌ **数据只在本地**，换设备/清缓存即丢失，无多设备同步
- ❌ 无法做需要服务端的功能（语音转写 Whisper、管理员看板、用量统计）

> 若未来要恢复多设备同步，需重新引入服务端——但**不要**再退回 Render 免费实例，30s 超时是当初的根本痛点。

---

## 三、文件结构

仓库**只有三个文件**：

```
zhaoxi-request-tool/
├── index.html          # 整个应用
├── 项目交接文档.md      # 本文件
└── .gitignore
```

本地工作目录另有未纳入 git 的遗留文件（`.env`、`supabase password.txt`、`start.bat`、`需求工具测试.txt`），均为旧后端残留，可忽略。

> 曾经的 `main.py` / `Procfile` / `requirements.txt` / `static/` 已于 2026-08-05 删除。
> 其中 `static/index.html` 曾与根目录 `index.html` **字节级重复**，是历史上最容易改错文件的坑，现已消除。

---

## 四、外部依赖（全部走 CDN，无构建步骤）

| 库 | 用途 | 加载时机 |
|---|---|---|
| `mermaid@10` | 流程图/线框图渲染 | 页面加载即引入 |
| `tailwindcss`（CDN 版） | 少量样式 | 页面加载即引入 |
| `html2canvas@1.4.1` | 页面展示导出为图片 | 按需 |
| `mammoth@1.6.0` | 解析 .docx | 按需 |
| `xlsx@0.18.5`（SheetJS） | 解析 .xlsx / .xls | 按需 |
| `jszip@3.10.1` | 解析 .pptx（pptx 本质是 zip） | 按需 |

按需加载统一走 `_loadScript(src, globalName)`（L3266），已存在的全局变量直接跳过。

> **风险**：全部依赖 CDN，内网或 CDN 被拦截时对应功能直接失效，**无本地兜底**。

---

## 五、AI 调用层

浏览器直连 OpenAI 兼容接口，**没有任何中间层**。

| 项 | 默认值 | 用户可覆盖 |
|---|---|---|
| 接口地址 | `https://api.deepseek.com` | ✅ localStorage `wolfai_api_url` |
| 模型 | `deepseek-v4-pro` | ✅ localStorage `wolfai_api_model` |
| API Key | **无默认，必须用户自填** | ✅ localStorage `wolfai_api_key` |

> 💡 localStorage 的 key 名仍带历史前缀 `wolfai_`（早期用 wolfai.top 代理），与当前 DeepSeek 无关，**改名会导致老用户配置丢失，建议保持不动**。

| 函数 | 位置 | 说明 |
|---|---|---|
| `_callAI(messages, maxTokens)` | L1508 | 非流式，返回完整 content |
| `_callAIStream(messages, maxTokens, onChunk)` | L1528 | SSE 流式，逐块回调 `onChunk(delta, accumulated)` |

各处 `max_tokens`：对话 2048、流程图 1500–2500、线框图 4096、Demo 6000。

---

## 六、访谈提示词体系（改这块前必读）

### 6.1 提示词是怎么拼出来的

系统提示词**不是一个常量**，而是每轮由 `_buildSystemPrompt()`（L1746）动态拼装：

```
SYSTEM_PROMPT_BASE (L1663)   固定的角色、方法论、输出格式
        +
【框架门禁段】               取决于 frameworkConfirmed
        +
【已确认信息锚点段】          取决于 currentConfirmed
```

后两段是**运行时状态**，这是整套设计的关键，下面分别说明。

### 6.2 八大维度

```js
const DIMENSIONS = ['角色权限','触发条件','输入数据','处理流程',
                    '审批链路','输出结果','异常处理','合规要求'];  // L1473
```

它同时是三样东西：**追问清单**、**完整度评分锚点**、**记忆锚点的键**。

### 6.3 记忆锚点：为什么 AI 不会重复提问

对话历史被 `history.slice(-8)` 硬截断，第 5 轮后最初的背景就滑出窗口了。
解决办法是让 AI 每轮在 JSON 里回吐一份八维度已确认摘要（`confirmed` 字段），
前端存进 `currentConfirmed`，下一轮由 `_buildSystemPrompt()` 拼回提示词。

> 效果：即使历史被截断，已确认的事实也不丢，同时天然消除重复追问。
> **改动注意**：`confirmed` 必须每轮输出完整的八个键，AI 只回增量会导致信息逐轮丢失。

### 6.4 ★ 框架确认闸门（framing）★

**这是最容易被误删的一处设计，务必读完再改。**

**要解决的问题**：用户反馈 AI 听完故事就一头扎进某个细节，揪着枝节不放。

**根因有两层**：
1. 提示词曾指示「每轮只挑一个维度追问」= 深度优先，而阶段切换又绑定在"已填几个维度"上——**填格子本身就是深挖**，根本没有先摸清全貌的环节；
2. 更根本的是，**八大维度描述的是「一件事」的八个侧面**。真实需求常含多个子模块，此时问「审批链路是什么」本身就有歧义，模型只能挑一个默认的钻下去。

**解决办法**：在 exploring 与 drilling 之间插入 `framing` 阶段。

```
opening → exploring → framing → drilling → summarizing
                        ↑
             停止追问细节，拎出主干+分支
             交用户勾选、排序、确认
```

**⚠️ 为什么前端要维护 `frameworkConfirmed` 状态（别把它当冗余删掉）**

提示词里已经写了「未确认框架前严禁 drilling」，但**光靠文字约束模型憋不住，仍会提前深挖**。
所以 `_buildSystemPrompt()` 会根据前端状态注入不同的硬指令：

```js
if (!frameworkConfirmed) {
  // 注入：严禁输出 stage="drilling"，严禁深挖任何单个维度的细节
} else {
  // 注入：已确认的分支顺序 + 当前应聚焦哪个分支 + 未列入的不要再问
}
```

**这个前端状态是门禁真正生效的原因，删掉它门禁就废了。**

**相关状态变量**（L1480–1483、L1905）：

| 变量 | 含义 |
|---|---|
| `currentFramework` | AI 给出的原始框架 `{main, branches:[{id,name,desc}]}` |
| `frameworkConfirmed` | 门禁开关，false 时禁止 drilling |
| `branchPlan` | 用户确认后的深挖计划（已过滤已排序） |
| `currentBranchIdx` | 当前深挖到第几个分支 |
| `fwDraft` | 卡片上的勾选/排序中间态 |

**框架确认卡**（`renderFrameworkCard` L1908 起）：主干 + 分支列表，可勾选纳入/排除、可 ↑↓ 调序。
用户点确认后 → `confirmFramework()`（L1982）生成 `branchPlan`、打开门禁、锁定卡片留档，
并**自动发起下一轮**（调 `sendMessage(msg)` 把确认结果作为一条用户消息进入历史）。

### 6.5 完整度是怎么算的

**不采信 AI 给的数字**（同一段对话重跑两次它能给出完全不同的值）。
由 `updateCompletenessFromConfirmed()`（L2313）计算：

```
completeness = 20% × 框架已确认 + 80% × (已确认维度数 / 8)
```

进度条 tooltip 会明示当前深挖到哪个分支、还差哪几个维度。
**降级**：若 AI 没返回 `confirmed` 且框架也未确认，才退回采用 `data.completeness`。

### 6.6 输出 JSON 格式（当前）

```json
{
  "message": "复述理解 + 下一个追问问题",
  "stage": "opening|exploring|framing|drilling|summarizing",
  "currentBranch": "b2",
  "framework": { "main": "...", "branches": [{"id":"b1","name":"...","desc":"..."}] },
  "confirmed": { "角色权限":"", "触发条件":"", ... 共八个键 }
}
```

- `framework` **仅在 stage=framing 时输出**，其余填 null
- `currentBranch` 仅在 stage=drilling 时填分支 id
- **`message` 必须排在最前面**——原因见下一节

---

## 七、流式打字是怎么实现的

AI 输出的是严格 JSON，流式传输时**任何中间时刻收到的都是半截、非法的 JSON**，
`JSON.parse` 必然失败。不能等解析完再显示，否则用户要盯着"正在输入"看几十秒。

`_extractPartialMessage(acc)`（L1870）手工扫描 `"message"` 后的字符串体，逐个还原转义
（`\n` `\"` `\uXXXX`），**尾部若遇到被 chunk 边界切断的转义序列就丢弃**，等下一块补齐。

它保证一条不变量：

> **任意时刻返回的部分结果，都是最终完整结果的前缀。**
> 因此打字过程永远不会出现乱码，也不会回退重绘。

**渲染流程：**

```
_callAIStream 的 onChunk
  → _extractPartialMessage(累积文本)
  → 首次有内容时 beginStreamingMessage() 建空气泡 (L2025)
  → 后续每帧覆写 bubble.innerHTML = fmt(partial)
流结束
  → _parseAIText(完整文本) 拿最终 JSON (L1836)
  → applyAIResponse(data, true, streamBubble) (L2042)
      ├─ streamBubble 存在 → 覆写它，移除 #streaming-msg 的 id（不新建气泡）
      └─ streamBubble 为空 → 走老路 addMessage()
```

**⚠️ 已知风险**：`message` 必须是 JSON 第一个字段。若模型误把 `confirmed` 放前面，
打字要等到约 83% 的内容到达后才开始（实测半截 JSON 173 字符中的第 143 字符）。
提示词已双重要求字段顺序，且 schema 示例把 `message` 放首位、`confirmed` 放末位，
但这是**概率约束不是保证**。最坏退化成"转圈后一次性出字"，不会崩。

**改这块的注意事项：**
1. `applyAIResponse` 第三个参数 `streamBubble` 可选，开场白调用只传两个参数属正常。
2. 出错路径必须同时调 `removeTyping()` 和 `removeStreamingMessage()`，否则残留空气泡。
3. 若改了输出字段名 `message`，必须同步改 `_extractPartialMessage` 里的 `'"message"'` 与偏移量 `+9`。

---

## 八、功能模块

### 8.1 对话访谈
- 开场白 `LOCAL_OPENING`（L1648）本地直出，无需网络；**引导用户讲真实故事并给出范例**，而不是填五维度表格
- 快捷卡片 `GRILL_CHIPS`（L1574）按 stage 切换；opening 阶段的 15 个场景模板均为**带时间/人/数字/卡点的真实叙事**（刻意避开"我想要一个XX系统"的框架，那正是方法论要避免的）
- `newSession()` 复用 `LOCAL_OPENING`，不要再各写一份

### 8.2 右侧面板主 Tab
`switchTab(name)` — `methodology` / `requirements` / `definition` / `diagram`

### 8.3 需求清单的 5 个子 Tab

⚠️ **命名陷阱，极易搞混：**

| 界面显示 | 子 Tab id | 生成调用 |
|---|---|---|
| 📝 需求梳理 | `summary` | `generateSection('summary')` |
| 📊 **流程图** | `wireframe` | `generateSection('wireframe')` |
| 🖥️ 页面展示 | `preview` | `generateSection('preview')` |
| 📄 PRD文档 | `prd` | `generatePRDInline()` |
| 🔲 **线框图** | `ui-wireframe` | `generateSection('ui_wireframe')` |

> 两处反直觉：
> 1. id 叫 `wireframe` 的显示**「流程图」**，id 叫 `ui-wireframe` 的才是**「线框图」**；
> 2. 子 Tab 用连字符 `ui-wireframe`，生成类型用下划线 `ui_wireframe`，两者不能互换。

### 8.4 文件上传（L3313）

| 格式 | 解析方式 |
|---|---|
| `.txt` `.md` | 原生 `file.text()` |
| `.docx` | mammoth.js |
| `.xlsx` `.xls` | SheetJS |
| `.pptx` | `_extractPptxText()`（L3289），JSZip 解压后读 `ppt/slides/slideN.xml` |
| `.ppt` `.doc` | 明确报错，提示另存为新格式（二进制旧格式浏览器端无解） |

> **pptx 解析关键点**：PowerPoint 会把同一段落拆成多个 `<a:r>` run（改个字号就断开）。
> 必须**先按 `<a:p>` 段落分组、段内 run 直接拼接**，否则 `"30′"` 会被拆成 `"3"` 和 `"0′"`。
> 提取内容统一截断到 **8000 字**，百页 PPT 会被大幅截掉。

### 8.5 语音输入（L3397）

- **仅 Web Speech API 单引擎**，只有 Chrome / Edge / Android 支持
- Safari / iOS / Firefox：**点击时立即提示**改用 Chrome 或输入法自带语音，不再让用户白录
- 旧的 MediaRecorder + 后端 Whisper 兜底已随后端下线整体删除

### 8.6 会话管理
纯 localStorage，**无服务端同步**。`syncServerSessions()` / `saveToServer()` 是保留的空函数。

### 8.7 登录
⚠️ **登录是装饰性的**：`loginUser()`（L4821）只校验邮箱格式，token 是 `'local_' + Date.now()`，
**无密码、无验证、无服务端**。作用仅是显示用户名 + 切换邮箱时清空本地会话。

---

## 九、数据结构

### 9.1 localStorage 键名

| Key | 用途 |
|---|---|
| `zhaoxi_token` | 伪登录 token |
| `zhaoxi_user` | `{email, name}` |
| `zhaoxi_req_sessions` | 会话列表（**全部数据都在这一个 key 里**） |
| `zhaoxi_autosave` | 当前未保存会话的自动暂存 |
| `wolfai_api_key` / `wolfai_api_url` / `wolfai_api_model` | API 配置 |

### 9.2 会话对象

```javascript
{
  id, name, savedAt,
  history: [{role, content}],
  requirements: [],
  mermaid, wireframeMermaid, uiWireframeMermaid,
  prdContent,
  // ── 访谈状态（2026-08 新增，四处存取点必须同步）──
  confirmed,           // 八大维度已确认摘要
  framework,           // AI 给出的原始框架
  frameworkConfirmed,  // 门禁开关
  branchPlan,          // 用户确认后的深挖顺序
  currentBranchIdx,    // 当前深挖到第几个
  completeness
}
```

> **四处存取点**：`autoSave()` L4085、`saveCurrentSession()` L4107、`loadSession()` L4147、
> 页面加载时的暂存恢复 L4430 左右。**加字段必须四处都改**，漏一处就会出现"存进去读不出来"。

---

## 十、核心函数索引

> 行号对应 commit `a32f1d2`，改动后会漂移，建议按函数名搜索。

| 函数 | 位置 | 作用 |
|---|---|---|
| **提示词与访谈状态** | | |
| `_buildSystemPrompt()` | L1746 | 拼装系统提示词（基座 + 门禁 + 记忆锚点） |
| `_confirmedCount(c)` | L2303 | 已确认维度数，不可用时返回 -1 |
| `updateCompletenessFromConfirmed()` | L2313 | 按 20/80 公式算完整度并更新 tooltip |
| **框架闸门** | | |
| `renderFrameworkCard(fw)` | L1908 | 渲染框架确认卡 |
| `paintFrameworkCard()` | L1929 | 重绘卡片（勾选/排序后） |
| `toggleFwBranch(i)` / `moveFwBranch(i,d)` | L1962 / L1967 | 勾选、调序 |
| `confirmFramework()` | L1982 | 确认框架、开门禁、自动发起下一轮 |
| `updateBranchLabel()` | L2016 | 状态栏「深挖中 2/3」 |
| **对话与流式** | | |
| `sendMessage(overrideText?)` | L1781 | 发消息主流程；传参可程序化触发 |
| `_extractPartialMessage(acc)` | L1870 | 从半截 JSON 增量提取 message |
| `beginStreamingMessage()` | L2025 | 创建流式填充气泡 |
| `_parseAIText(raw)` | L1836 | 完整 JSON 解析（三次尝试 + fallback） |
| `applyAIResponse(data, add, bubble)` | L2042 | 渲染回复、更新各状态 |
| **物料生成** | | |
| `generateSection(type)` | L2766 | 物料生成总入口 |
| `generateWireframeStream()` | L2834 | 流式生成流程图 |
| `generateUiWireframeStream()` | L2903 | 流式生成线框图 |
| `generatePRDInline()` | L3220 | 生成 PRD |
| `generateDemo()` | L3159 | 生成可交互 HTML 原型 |
| **文件与语音** | | |
| `handleFileSelect(event)` | L3313 | 附件解析入口 |
| `_extractPptxText(buf)` | L3289 | pptx 文本提取 |
| `_loadScript(src, global)` | L3266 | 按需加载 CDN 脚本 |
| `toggleVoice()` | L3397 | 语音输入 |
| **会话** | | |
| `autoSave()` / `saveCurrentSession()` | L4085 / L4107 | 会话保存 |
| `loadSession(id)` | L4147 | 加载历史会话 |
| `newSession()` | L4301 | 新建（需重置全部访谈状态） |
| `downloadAllZip()` | L3966 | 打包下载全部交付物 |

---

## 十一、已知问题与技术债

### 🔴 待确认（需用真实 API Key 实跑验证）

- [ ] **模型对新 schema 的配合度**：是否老实在 framing 阶段输出 `framework` 字段、
      分支拆得是否合理（提示词限定 2–5 个、相对独立）。拆得太碎或太粗时调整该约束即可
- [ ] **框架反复重画**：用户若持续提意见，AI 可能不断重出 framing。已做两层防护
      （同时只保留一张待确认卡；确认按钮始终可用）。若实际绕圈，可加"第 N 次后强制进入 drilling"

### 🟠 P1 — 数据与文案

- [ ] **登录弹窗文案是假的**：写着「您的会话记录将安全保存在云端，支持跨设备查看」，
      实际是纯 localStorage，换设备就没了。**会让用户误以为数据有保障而不做备份**
- [ ] **localStorage 无配额保护**：`saveSessions()` 没有 try/catch，超 5MB 时
      `QuotaExceededError` 静默打断保存，用户毫无察觉
- [ ] **无会话导出/导入**：清浏览器数据 = 全部资料蒸发，没有任何补救手段
- [ ] **`uiWireframeSpec` 手动保存时丢失**：`autoSave()` 写了它（L4094）、`loadSession()`
      读了它（L4202），但 `saveCurrentSession()` **从不写**——手动点「保存」的会话，
      线框图的 [SPEC] 说明文本会丢
- [ ] **幽灵请求**：`loadSession()` 在 `userToken` 存在时仍会 `fetch('/api/sessions/{id}')`，
      在 GitHub Pages 上必定 404，先弹「加载中」再静默降级
- [ ] **生成物料的上下文截断**：`_buildHistoryText` 用 `slice(-20)`，与对话的 `slice(-8)`
      不一致。对话侧已由记忆锚点解决，但生成物料时仍可能丢失早期信息

### 🟡 P2 — 代码质量

- [ ] **34 处 `alert()`** 阻塞弹窗，与 `showToast()` 混用，风格割裂
- [ ] **三套高度重复的图表代码**（flowchart / wireframe / ui_wireframe），渲染 + 缩放 + 导出各复制一遍，约 600 行
- [ ] **无 `AbortController`**：生成中途无法取消，也没有超时兜底
- [ ] **Tailwind CDN + 200+ 处内联 `style=`**：控制台持续告警，且几乎没真正用到 Tailwind
- [ ] **CDN 无本地兜底**：内网环境下 Mermaid / 文件解析功能直接消失
- [ ] `exportFlowchartPNG()` / `exportFlowchartMermaid()` 仍在代码中但已无入口

### ✅ 已解决（勿重复提工单）

- ~~流式打字未生效~~ → `_extractPartialMessage` 已打通（2026-08-05）
- ~~PPT 上传必报错~~ → JSZip 解析已加（2026-08-05）
- ~~语音在非 Chrome 白录~~ → 改为点击即提示（2026-08-05）
- ~~对话上下文截断致重复提问~~ → 记忆锚点已解决（2026-08-05）
- ~~完整度数字无依据~~ → 改为按维度计算（2026-08-05）
- ~~AI 过早深挖细节~~ → 框架确认闸门（2026-08-05）

---

## 十二、开发与部署

**本地预览**：直接双击 `index.html` 即可，无需服务器或构建。
（若需 http 环境，本机 `.claude/launch.json` 已配 `需求打磨工具` → 端口 8772）

**部署**：

```bash
git add index.html
git commit -m "fix: xxx"
git push
```

推送到 `main` 后 GitHub Pages 自动发布，**约 30 秒生效**。
**没有测试环境，push 即上线**，改动前请自行验证。

**改动 index.html 后的自查清单：**

1. **JS 语法检查**——把 `<script>` 块抽出来跑 `node --check`：
   ```bash
   sed -n '/^<script>$/,/^<\/script>$/p' index.html | sed '1d;$d' > /tmp/app.js && node --check /tmp/app.js
   ```
2. 本地打开，发一条消息：确认**逐字打字**、**只出现一个气泡**
3. 若动了访谈逻辑：确认 AI **先出框架卡**再深挖，勾选/调序/确认全链路可用
4. 若加了会话字段：**四处存取点**都要改（见 9.2）
5. 控制台无新增报错

**⚠️ 声明顺序陷阱**：开场白 `applyAIResponse(LOCAL_OPENING)` 在脚本中段（L1656）
就会执行。任何它依赖的 `const`（如 `DIMENSIONS` L1473）**必须声明在其之前**，
否则页面加载时抛 TDZ 错误、整个应用白屏。这也是 `DIMENSIONS` 被刻意放在文件顶部的原因。

**commit 规范：**

```
fix:      修复 Bug
feat:     新功能
style:    UI/文案调整
refactor: 代码重构
docs:     文档
chore:    杂项
```

---

## 十三、改动史

| 日期 | commit | 内容 |
|---|---|---|
| 2026-08-05 | `a32f1d2` | 新增框架确认闸门（framing 阶段 + 前端门禁 + 确认卡片 + 分支追踪） |
| 2026-08-05 | `28325e6` | 重写访谈提示词：恢复方法论、记忆锚点、完整度按维度计算、开场故事化 |
| 2026-08-05 | `c290b13` | 按纯前端架构重写本文档 |
| 2026-08-05 | `95b3d23` | 删除后端残留（main.py / Procfile / requirements.txt / static/） |
| 2026-08-05 | `d51e425` | 打通流式打字、修复 PPT 上传、修复语音降级提示 |
| 2026-06 | `24f6fd7` | 去掉 Cloudflare 代理，改浏览器直连 API；登录改为纯本地 |

---

*接手者建议从第六节（访谈提示词体系，尤其 6.4 框架闸门）和第八节的命名陷阱读起，这两处最容易踩坑。*
