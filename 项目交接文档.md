# 朝曦家办 · 需求打磨工作台 — 项目交接文档

> 更新时间：2026-08-05
> 架构：**纯静态前端**（无后端、无数据库）
>
> ⚠️ 本文档上一版（2026-04-21）描述的 FastAPI + Supabase + Render 架构**已于 6 月被整体拆除**，若你看到的是旧版请以本版为准。

---

## 一、项目概览

| 项 | 内容 |
|---|---|
| 产品名称 | 朝曦家办 · 需求打磨工作台（zhaoxi-request-tool） |
| 用途 | 内部工具。业务团队通过 AI 深度访谈，把模糊想法转成可交付 IT 的需求规格 |
| 线上地址 | https://annayuqianyu-crypto.github.io/zhaoxi-request-tool/ |
| 代码仓库 | https://github.com/annayuqianyu-crypto/zhaoxi-request-tool |
| 部署方式 | GitHub Pages，从 `main` 分支根目录直接发布（**仓库内无 CI 配置**，在 GitHub 仓库 Settings → Pages 中设置） |

---

## 二、架构：为什么现在没有后端

项目经历过三次架构迁移，理解这段历史能避免走回头路：

```
① FastAPI + Supabase + Render      →  Render 免费实例 30s 超时，长对话必 502
② 前端 + Cloudflare Worker 代理     →  多一跳、多一处要维护的密钥
③ 纯静态前端，浏览器直连 LLM API    →  当前架构
```

**当前架构的取舍：**

- ✅ 零运维、零服务器成本、无冷启动、不受 30s 超时限制
- ✅ 部署 = `git push`，GitHub Pages 自动发布
- ❌ **每位用户必须自己填 API Key**（存在各自浏览器的 localStorage）
- ❌ **数据只在本地**，换设备/清缓存即丢失，无多设备同步
- ❌ 无法做需要服务端的功能（语音转写 Whisper、管理员看板、用量统计）

> 若未来要恢复多设备同步或统一密钥，需重新引入服务端——但**不要**再退回 Render 免费实例，30s 超时是当初的根本痛点。

---

## 三、文件结构

仓库**只有两个文件**：

```
zhaoxi-request-tool/
├── index.html      # 整个应用：HTML + CSS + JS 全内联，约 4500 行
└── .gitignore
```

**本地工作目录还有这些未纳入 git 的文件**（仅本机参考，不会上传）：

```
.env                    # 旧后端遗留的环境变量
supabase password.txt   # 旧凭据（已加入 .gitignore）
start.bat               # 旧后端本地启动脚本
需求工具测试.txt          # 测试记录
项目交接文档.md          # 本文件
```

> ⚠️ 曾经存在的 `main.py` / `Procfile` / `requirements.txt` / `static/` 已于 2026-08-05 全部删除（旧后端残留，无任何调用）。
> 其中 `static/index.html` 曾与根目录 `index.html` **字节级重复**，是历史上最容易改错文件的坑，现已消除。

---

## 四、外部依赖（全部走 CDN，无构建步骤）

| 库 | 用途 | 加载时机 |
|---|---|---|
| `mermaid@10` | 流程图/线框图渲染 | 页面加载即引入 |
| `tailwindcss`（CDN 版） | 少量样式 | 页面加载即引入 |
| `html2canvas@1.4.1` | 页面展示导出为图片 | 按需 |
| `mammoth@1.6.0` | 解析 .docx | 按需 |
| `xlsx@0.18.5`（SheetJS） | 解析 .xlsx / .xls | 按需 |
| `jszip@3.10.1` | 解析 .pptx（pptx 本质是 zip） | 按需 |

按需加载统一走 `_loadScript(src, globalName)`（[index.html:2942](index.html#L2942)），已存在的全局变量会直接跳过重复加载。

> **风险**：全部依赖 CDN，内网或 CDN 被拦截时对应功能直接失效，目前**无本地兜底**。

---

## 五、AI 调用层

浏览器直连 OpenAI 兼容接口，**没有任何中间层**。

| 项 | 默认值 | 用户可覆盖 |
|---|---|---|
| 接口地址 | `https://api.deepseek.com` | ✅ localStorage `wolfai_api_url` |
| 模型 | `deepseek-v4-pro` | ✅ localStorage `wolfai_api_model` |
| API Key | **无默认，必须用户自填** | ✅ localStorage `wolfai_api_key` |

> 💡 localStorage 的 key 名仍带历史前缀 `wolfai_`（早期用 wolfai.top 代理），与当前 DeepSeek 无关，改名会导致老用户配置丢失，**建议保持不动**。

**两个核心调用函数：**

| 函数 | 位置 | 说明 |
|---|---|---|
| `_callAI(messages, maxTokens)` | [index.html:1461](index.html#L1461) | 非流式，返回完整 content 字符串 |
| `_callAIStream(messages, maxTokens, onChunk)` | [index.html:1481](index.html#L1481) | SSE 流式，逐块回调 `onChunk(delta, accumulated)` |

---

## 六、流式打字是怎么实现的（改动前请务必读这段）

这是全项目**最反直觉的一处设计**。

SYSTEM_PROMPT 要求 AI 只输出严格 JSON：

```json
{"message":"AI回应+追问","stage":"exploring","completeness":45}
```

流式传输时，任何中间时刻收到的都是**半截、非法的 JSON**，`JSON.parse` 必然失败。所以不能等解析完再显示，否则用户要盯着"正在输入"看几十秒。

解决办法是 `_extractPartialMessage(acc)`（[index.html:1722](index.html#L1722)）：手工扫描 `"message"` 后面的字符串体，逐个还原转义（`\n` `\"` `\uXXXX`），**尾部若遇到被 chunk 边界切断的转义序列就直接丢弃**，等下一块补齐。

它保证一条不变量：

> **任意时刻返回的部分结果，都是最终完整结果的前缀。**
> 因此打字过程永远不会出现乱码、也不会回退重绘。

渲染流程：

```
_callAIStream 的 onChunk
  → _extractPartialMessage(累积文本)
  → 首次有内容时 beginStreamingMessage() 建一个空气泡
  → 后续每帧覆写 bubble.innerHTML = fmt(partial)
流结束
  → _parseAIText(完整文本) 拿到最终 JSON
  → applyAIResponse(data, true, streamBubble)
      ├─ streamBubble 存在 → 覆写它，并移除 #streaming-msg 的 id（不新建气泡）
      └─ streamBubble 为空 → 走老路 addMessage()
```

**改这块时的注意事项：**

1. `applyAIResponse` 的第三个参数 `streamBubble` 是可选的，另外两处调用（开场白）只传两个参数，属正常。
2. 出错路径必须同时调 `removeTyping()` 和 `removeStreamingMessage()`，否则会残留空气泡。
3. 若改了 SYSTEM_PROMPT 的输出字段名（`message`），必须同步改 `_extractPartialMessage` 里的 `'"message"'` 与偏移量 `+9`。

---

## 七、功能模块

### 7.1 对话访谈
- 左侧对话区，AI 逐步追问，每轮更新 `stage` 与 `completeness`
- 上下文截断：`history.slice(-8)`（[index.html:1659](index.html#L1659)）
- 快捷回复卡片 `GRILL_CHIPS`（[index.html:1527](index.html#L1527)），按 stage 切换
- 开场白 `LOCAL_OPENING`（[index.html:1601](index.html#L1601)），本地直出，无需网络

### 7.2 右侧面板：主 Tab

`switchTab(name)` — `methodology`（方法论）/ `requirements`（需求清单）/ `definition`（功能定义）/ `diagram`

### 7.3 需求清单的 5 个子 Tab

⚠️ **命名陷阱，极易搞混：**

| 界面显示 | 子 Tab id | 生成调用 | 产出 |
|---|---|---|---|
| 📝 需求梳理 | `summary` | `generateSection('summary')` | 结构化需求 JSON 数组 |
| 📊 **流程图** | `wireframe` | `generateSection('wireframe')` | Mermaid 功能流程设计图 |
| 🖥️ 页面展示 | `preview` | `generateSection('preview')` | 仿真界面 |
| 📄 PRD文档 | `prd` | `generatePRDInline()` | Markdown 规格说明书 |
| 🔲 **线框图** | `ui-wireframe` | `generateSection('ui_wireframe')` | Mermaid 线框图 + [SPEC] 说明 |

> 注意两处反直觉：
> 1. id 叫 `wireframe` 的显示的是**「流程图」**，id 叫 `ui-wireframe` 的才是**「线框图」**；
> 2. 子 Tab 用连字符 `ui-wireframe`，生成类型用下划线 `ui_wireframe`，两者不能互换。

### 7.4 文件上传（[index.html:2989](index.html#L2989)）

| 格式 | 解析方式 |
|---|---|
| `.txt` `.md` | 原生 `file.text()` |
| `.docx` | mammoth.js |
| `.xlsx` `.xls` | SheetJS |
| `.pptx` | `_extractPptxText()`（[index.html:2965](index.html#L2965)），JSZip 解压后读 `ppt/slides/slideN.xml` |
| `.ppt` `.doc` | 明确报错，提示另存为新格式（二进制旧格式浏览器端无解） |

> **pptx 解析的关键点**：PowerPoint 会把同一段落拆成多个 `<a:r>` run（改个字号就断开）。
> 必须**先按 `<a:p>` 段落分组、段内 run 直接拼接**，否则 `"30′"` 会被拆成 `"3"` 和 `"0′"`。
> 提取内容统一截断到 **8000 字**，长文档（如百页 PPT）会被大幅截掉。

### 7.5 语音输入（[index.html:3073](index.html#L3073)）

- **仅 Web Speech API 单引擎**，只有 Chrome / Edge / Android 支持
- Safari / iOS / Firefox：**点击时立即提示**改用 Chrome 或输入法自带语音，不再让用户白录
- 旧的 MediaRecorder + 后端 Whisper 兜底路径已随后端下线整体删除

### 7.6 会话管理

纯 localStorage，**无服务端同步**。`syncServerSessions()` / `saveToServer()` 是保留的空函数。

### 7.7 登录

⚠️ **登录是装饰性的**：`loginUser()`（[index.html:4463](index.html#L4463)）只校验邮箱格式，token 是 `'local_' + Date.now()`，**无密码、无验证、无服务端**。作用仅是显示用户名 + 切换邮箱时清空本地会话。

---

## 八、数据结构

### 8.1 localStorage 键名

| Key | 用途 |
|---|---|
| `zhaoxi_token` | 伪登录 token |
| `zhaoxi_user` | `{email, name}` |
| `zhaoxi_req_sessions` | 会话列表（**全部数据都在这一个 key 里**） |
| `zhaoxi_autosave` | 当前未保存会话的自动暂存 |
| `wolfai_api_key` / `wolfai_api_url` / `wolfai_api_model` | API 配置 |

### 8.2 会话对象

```javascript
{
  id, name, savedAt,
  history: [{role, content}],   // 对话记录
  requirements: [],             // 结构化需求数组
  mermaid: '',                  // 业务流程图
  wireframeMermaid: '',         // 功能流程设计图
  uiWireframeMermaid: '',       // 线框图
  uiWireframeSpec: '',          // 线框图的 [SPEC] 说明文本
  prdContent: '',               // PRD 全文
  completeness: 0
}
```

### 8.3 需求对象

```javascript
{ module, feature, description, wireframe, process: [], inputs: [], outputs: [] }
```

---

## 九、核心函数索引

> 行号对应 2026-08-05 版本，改动后会漂移，建议按函数名搜索。

| 函数 | 位置 | 作用 |
|---|---|---|
| `sendMessage()` | [1613](index.html#L1613) | 发消息主流程，含流式渲染 |
| `_extractPartialMessage(acc)` | [1722](index.html#L1722) | 从半截 JSON 增量提取 message |
| `beginStreamingMessage()` | [1754](index.html#L1754) | 创建流式填充气泡 |
| `_parseAIText(raw)` | [1688](index.html#L1688) | 完整 JSON 解析（三次尝试 + fallback） |
| `applyAIResponse(data, addToHistory, streamBubble)` | [1771](index.html#L1771) | 渲染回复、更新 stage/完整度 |
| `generateSection(type)` | [2442](index.html#L2442) | 物料生成总入口 |
| `generateWireframeStream()` | [2510](index.html#L2510) | 流式生成流程图 |
| `generateUiWireframeStream()` | [2579](index.html#L2579) | 流式生成线框图 |
| `generatePRDInline()` | [2896](index.html#L2896) | 生成 PRD |
| `generateDemo()` | [2835](index.html#L2835) | 生成可交互 HTML 原型 |
| `handleFileSelect(event)` | [2989](index.html#L2989) | 附件解析入口 |
| `_extractPptxText(buf)` | [2965](index.html#L2965) | pptx 文本提取 |
| `_loadScript(src, global)` | [2942](index.html#L2942) | 按需加载 CDN 脚本 |
| `toggleVoice()` | [3073](index.html#L3073) | 语音输入 |
| `autoSave()` / `saveCurrentSession()` | [3761](index.html#L3761) / [3778](index.html#L3778) | 会话保存 |
| `loadSession(id)` | [3813](index.html#L3813) | 加载历史会话 |
| `renderMermaidInto(...)` | [2097](index.html#L2097) | Mermaid 渲染（两次尝试：原始 + 清洗） |
| `downloadAllZip()` | [3642](index.html#L3642) | 打包下载全部交付物 |

---

## 十、已知问题与技术债

### 🟠 P1 — 数据与稳定性

- [ ] **localStorage 无配额保护**：`saveSessions()` 没有 try/catch，超出 5MB 时 `QuotaExceededError` 会静默打断保存，用户不会收到任何提示
- [ ] **无会话导出/导入**：清浏览器数据 = 全部资料蒸发，目前没有任何补救手段
- [ ] **幽灵请求**：`loadSession()`（[index.html:3813](index.html#L3813)）在 `userToken` 存在时仍会 `fetch('/api/sessions/{id}')`，在 GitHub Pages 上必定 404，先弹「📂 加载中…」再静默降级，属多余往返 + 误导性提示
- [ ] **上下文截断不一致**：对话用 `slice(-8)`，生成物料用 `_buildHistoryText` 的 `slice(-20)`，且都不保留开场背景，访谈 10 轮后 AI 已不记得最初的部门与痛点

### 🟡 P2 — 代码质量

- [ ] **34 处 `alert()`** 阻塞弹窗，与 `showToast()` 混用，交互风格割裂
- [ ] **三套高度重复的图表代码**（flowchart / wireframe / ui_wireframe），渲染 + 缩放 + 导出各复制一遍，约 600 行
- [ ] **无 `AbortController`**：生成中途无法取消，也没有超时兜底
- [ ] **`max_tokens` 封顶 2000–3000**：复杂需求的 PRD / 线框图存在截断风险
- [ ] **Tailwind CDN + 236 处内联 `style=`**：控制台持续告警 `cdn.tailwindcss.com should not be used in production`，且实际几乎没用到 Tailwind
- [ ] **CDN 无本地兜底**：内网环境下 Mermaid / 文件解析功能会直接消失
- [ ] `exportFlowchartPNG()` / `exportFlowchartMermaid()` 仍在代码中但已无入口

---

## 十一、开发与部署约定

**本地预览**：直接双击 `index.html` 用浏览器打开即可，无需任何服务器或构建。

**部署**：

```bash
git add index.html
git commit -m "fix: xxx"
git push
```

推送到 `main` 后 GitHub Pages 自动发布，约 1 分钟生效。**没有测试环境，push 即上线**，改动前请自行在本地打开验证。

**改动 index.html 后的自查清单：**

1. JS 语法检查（把 `<script>` 块抽出来跑 `node --check`）
2. 本地浏览器打开，发一条消息确认打字效果正常、只出现一个气泡
3. 检查控制台无新增报错

**commit 规范：**

```
fix:      修复 Bug
feat:     新功能
style:    UI/文案调整
refactor: 代码重构
docs:     文档
```

---

## 十二、2026-08-05 本次改动摘要

| 改动 | 说明 |
|---|---|
| 打通真流式打字 | 原先 `onChunk` 传 `null`，全部 token 收完才渲染；新增 `_extractPartialMessage` 实现逐字显示 |
| 修复 PPT 上传必报错 | `accept` 含 `.pptx` 但解析器不支持，选中必失败；新增 JSZip 解析，并修正 `accept` 与提示文案 |
| 修复语音白录 | 非 Chrome 浏览器原先要录完整段才提示不可用，改为点击即提示；删除 72 行失效的 MediaRecorder 兜底 |
| 删除后端残留 | `main.py` / `Procfile` / `requirements.txt` / `.env.example` / `static/` 全部移除，仓库从 8 个文件降到 2 个 |
| 收紧 .gitignore | `supabase password.txt` 原先未被忽略，处于随时可能被误提交的状态 |

---

*接手者建议从第六节（流式实现）和第七节的命名陷阱读起，这两处最容易踩坑。*
